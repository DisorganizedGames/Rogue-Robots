#pragma once
#include "ShaderInterop_Common.h"

struct SubmeshMetadata
{
    uint vertStart;
    uint vertCount;
    uint indexStart;
    uint indexCount;
    uint blendStart;
    uint blendCount;
};
